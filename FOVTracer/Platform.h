#pragma once

#ifndef WIN32_LEAN_AND_MEAN
#define WIN32_LEAN_AND_MEAN			// Exclude rarely-used items from Windows headers.
#endif

#ifndef NOMINMAX
#define NOMINMAX		// Exclude min max names from Windows headers.
#endif

#include <Windows.h>